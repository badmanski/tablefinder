%table.table.table-hover
  %tr
    %th= t(:name)
    %th= t(:email)
    %th= t(:phone)
    %th= t(:place, count: 1)
    %th= t(:role)
    %th
    %th

  - @users.each do |user|
    %tr
      %td= link_to user.name, user_path(user)
      %td= user.email
      %td= user.phone
      %td= link_to user.place_name, place_path(user.place) if user.place
      %td= user.role.capitalize
      %td= link_to t(:edit), edit_user_path(user), class: 'btn btn-default btn-sm'
      %td= link_to t(:delete), user, method: :delete, confirm: t(:sure), class: 'btn btn-danger btn-sm'

= link_to t(:new_user), new_user_path, class: 'btn btn-success'

:javascript
  $('.nav li').removeClass('active');
  $('#nav-users').addClass('active');
