%table.table.table-hover
  %tr
    %th= t(:name)
    %th= t(:email)
    %th= t(:phone)
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.email
      %td= user.phone
      %td= link_to t(:edit), edit_user_path(user)
      %td= link_to t(:delete), user, method: :delete, confirm: t(:sure)

= link_to t(:new_user), new_user_path, class: 'btn btn-success'

:javascript
  $('.nav li').removeClass('active');
  $('#nav-users').addClass('active');
